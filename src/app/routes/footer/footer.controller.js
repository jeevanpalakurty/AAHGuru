var _ = require('lodash');
require('angular');

module.exports = angular.module('aah-footer-controller-module', [

]).controller('aahFooterController', ['$state', function FooterController($state) {

	var ctrl = this,
		_showApps = false,
		_priorityState = false;

	_.extend(ctrl, {


	});
}]);